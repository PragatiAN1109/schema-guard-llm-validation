# GitHub Optimization — SchemaGuard

---

## Repository Description (appears under repo name)

Semantic validation and drift detection for LLM-generated structured outputs. Catches cross-field contradictions that JSON schema validation misses. Python + FastAPI + Streamlit.

---

## Topics / Tags

Add these via Settings → Topics on the GitHub repo page:

```
llm-validation
semantic-rules
drift-detection
json-schema
data-quality
fastapi
streamlit
python
healthcare
fintech
confidence-scoring
prompt-engineering
ai-safety
backend
```

---

## Pinned Project Summary (for GitHub profile)

**SchemaGuard** — Semantic compliance layer for LLM outputs. Validates JSON records with cross-field rules, computes confidence scores, detects distribution drift, and routes records to trusted/flagged/quarantined. FastAPI + Streamlit. 100% accuracy on seed data.

---

## Repo Highlights Section (for About sidebar)

If GitHub supports a highlights or features section:

- 10 semantic rules across healthcare + finance domains
- Multi-signal drift detection (z-score, PSI, null-rate)
- Confidence scoring with three-tier routing
- FastAPI REST API + Streamlit demo UI
- Full evaluation pipeline with metrics and charts

---

## Social Preview Image Text (if creating one)

```
SchemaGuard
━━━━━━━━━━━━━━━━━━━━━
Semantic Validation for LLM Outputs
Schema Check → Rules → Confidence → Decision
trusted | flagged | quarantined
```

---

## README Badges (already in README, verify they render)

```markdown
[![Python 3.11+](https://img.shields.io/badge/python-3.11+-blue.svg)]()
[![FastAPI](https://img.shields.io/badge/api-FastAPI-009688.svg)]()
[![License: MIT](https://img.shields.io/badge/license-MIT-green.svg)]()
```
